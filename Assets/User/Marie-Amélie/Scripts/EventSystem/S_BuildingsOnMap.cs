using System.Collections;
using System.Collections.Generic;
using UnityEngine;


[CreateAssetMenu(fileName = "Storage of buildings on map", menuName = "Data/Buildings on map")]
public class S_BuildingsOnMap : MonoBehaviour
{
    //Add listener on building placement so it's feeding a func with the building and depending on its type switch and add +1 to counter
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
